"""FalkorDB layer for the A3 PALIMPSEST arm.

`schema.py` is SHARED WITH THE DEMO by design (see memory/SCHEMA.md and
memory/taxonomy.py). The eval is not a side project: it is the removal test for
the demo's central claim, so it must exercise the same vocabulary.
"""
